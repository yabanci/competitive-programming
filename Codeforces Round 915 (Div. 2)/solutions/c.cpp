#include <iostream>
#include <cassert>

using namespace std;

const int mod = 1e9 + 7;

int solve() {
    int n;
    cin >> n;
    string s;
    cin >> s;
    return 0;
}
int main() {
#ifdef LOCAL
    assert(freopen("../input.txt", "r", stdin));
    // assert(freopen("../output.txt", "w", stdout));
#endif
    int t;
    cin >> t;
    while (t--) {
        solve();
    }
    return 0;
}
